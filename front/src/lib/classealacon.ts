import * from "d3";

type RoomData = {
    points: Number[][],
    name: string,
    capacity: number,
    projecteur: boolean,
};

export class classealacon {
    private rooms: Array<Number> = [];

    constructor(public name : String) {       
        
    }
}